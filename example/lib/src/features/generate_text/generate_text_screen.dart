import 'package:flutter/material.dart';

class GenerateTextScreen extends StatelessWidget {
  const GenerateTextScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
